8.12. torstai 

container and serveless computing in azure

perus kontainer linux palvelin päälle, multikontainer wordpress edusta ajetaan yksi tai useampaan kontaineriin ja siitä toisesta

vaatti käyttöjärjestelmän pyörimistä linux kanssa

cubernet nest azure

yritykset kompanktista, sovelluksen pyöriminen mikä on turvallinen ja oletuksena ei pääse toisten suorituksee ja on siirrettävä

docker host virtuaalikoneet host iaas ratkaisun ja pas palvelu, vaihteeks tekee ubuntu linux serveri päälle

multikontainer sovelluksen eri komponentti määritys teksti ja teksti on hyvin tarkka (yml) prkl..

valinnainen azure container registry - keskitetyn kontainer 

container engine - voi ajaa raudan päällä ja tuki on olemassa silloin saadaan container engine palvelu siihen mukaan tulee välittä käyttöjärjestelmän palvelu pyyntöä 
ja päin vastoin  ja suorituksen, mitä tahansa konen päälle ja ylimpänä sovellus tarvitsee binaries/libraries ja muita asentaa asioita 
mitä harjoitukseski on tehty web server ja ei tarvi muuta kuin varsinaine sovellus tai voi asentaa muu ja muuta palveluita dns palvelu.
oma verkko asetukset ja oma ip, endpoint to point yhteys ratkaisu 

docker terminology (docker engine) palvelu liikente käyttöjärjestelmän välillä, container image sen varsinaine docker ja voi ottaa konesta ulos kohti usb tikulla toiseen koneeseen
dockerfile - määritys tiedosto, asiota, se on aina dockerfile, windows server tulee vastaan!! 
docker registery - hallittaa laajempaa ympäristöä

docker host - ei vaadi mitään kuin virtuaalikone ja tarkistaa mukana docker laajennus, mahdollistsaa fontien suorittamista, powershell tai on kone asetusket peräisin template niin siinäkin laajenuksia, 
se on linux ja windows koneessa, kun sentaa ubuntu se tehdään template & azure marketplace saadaan virtuaalkone, windows tai ubuntu/linux paikallaa, erilaisia komponentia, 

yhteyden azure vm docker host - kun menee RDP, ssh, ja linux koneisiin

azurecr. io jos tulee useampi käyttäjä nii admin tili, on hyvä idea mutta azure pääkäyttäjä pystyy hallinnoida

azure serverless ei ole loogista, ei palvelu taustaa vaan suoraan pilvessä


luominen (logic app)
publish (workflow) tai docker containern, open mukaan workflow, plan type: standard - normaali yrityksen käyttöön resurssie enemmmän, consuption,
pricing workflow standard, 3.5GB, zone disabled

graafiset työkalu - workflows design, luo new workflow, stateful kuittaa kaikki prosessit mutta hitaampi,

designer - valitse triggeri, joku bullt in, add consultion eli luo operaattori --> azure file storage - copy file () --> 


funktio on triggeri voi olla web hub, ulkoista verkkoa ku haetaan sieltä dataa ja suoriuttuu funktio esim. virus tarkistus, ajastattu suorittaa funktioita ja voi kutsusa sovellukseksi
ohjelmointikieliä c#, nodejs, java,  python, php ja jne... , varsinainen koodaus & valitaan program stack

luoo valmiksi pohja tiedoston, jossa on nää erikoismerkit ja puuttu uvan koodit

integroituu tietokantaan, hyödyntää sisältöä voi toimia triggerinä ja event, mobiilisovellukset ja storage palvelu funktioita, ja azure ulkopuolisia github (webhooks), on-premises, twilio (sms)

kommunkointi palvelua, publish/subscribe, sovelluksen data välityksiä ja tarvitaan konnektoria, toimii etähallinta, tiedon välityksenä on-premises

kubernetes service (pages 302 -310) lisä

------------------------------------

azure ad 

luodaan käyttäjä ja oikeuksia, että testataan

identiteettinen ratkaisu microsoft pilvessä, tai  tää portaali tai saas palvelu, microsoft 365 palvelu niin on azure taustalla, jos on puhdas pilvi ympäristö niin sinne luodaan käyttäjät ja oikeuksia ja antaa per tai resurssi ryhmä

maanaging azure ad authentication and authorization, synkronoidi tai authorization, käyttäjä ei tarvi kahden ympäristöä ja salasanaa &

azure ad on kevyempi käyttäjiä ja rymiä, sovelluksia ja .. & erillainen ad, jos on ad premises on eri mutta paljo methodi & ja paljo kevempyä

on käyttäjiä ja resurssei & objektia, adds (tietokanta)

voi asentaa virtuaalikone päälle, mutta hyvin vähä tilausta on joku ad integroitu sovellus pyörii pilvessä ja vaatti domain kontrolleria esim. exchange edellyttää pilvessä on ad domain kontrolleri


synkronoiti on oma järjestelmä perus käyttäjä tunnus ja salasana, on muutakin muokkattavissa atribuutteja mitä viedä sinne ja jos on sovellus pilvessä azure mitä atribuutteja se tarvii

synkronoiti on toinen menetelmä, tapahtuu domain , permission domainia

active directory on permisiss keskeinen elementi ilman sitä mitään ei toimi ja j opa  ip aliverkko objektia jos fyysinen saitti rakentaa ja perus on authentication ja authorization  oikeuksien p yöriminen
perus objekti domain nimi ja koneet ja genery ryhmä, yksikköt ja muita objektia, on myös jopa ad sovelluksia edellyttää ad läsnä oloa, laajentaa perus ad:tä ja tuoo uusia objekti ja atribuutteja toista objektia lisää 

ad ds
hallinta GPO  tai bpsy ylläpitäjä asetuksia, pakottaa haluttuihin koheiteisin  esim. organisaation tai yksittäiseen kohteisiin, keskeinen menetelmä hallintaan ad palvelussa ja on security channel (luottosuuri) domain koneella,
mahdollista hallintaa, kun luodaan security channel hyppää domain paikallisiin paikkaan domain user ja jne. välttämätöntä domain palveluita näitä liitoksia ei ole, samoja group 

ad ds orgasanidoiaan yksiköidään palvelujen avulla group policy avulla, authentication voi laajentaa käyttämällä ad metsiä (on aina ad metsä) on useita pysty suuntaisia hieraktioita 
ja keskennään nimi avaruus, mutta saadaan useampi identtiejä luottosuhtella toiseen orgsaniaation laajennettua authetnitkointai

ad ds by using azure vms 
ad ympäristö signle tenant yhden organisaation hallussa ja siihen ei saa useita organisaatiota, azure ad voi luoda useita hakemistoja ja eri porukkan käyutössä, ja sitä ei toimi domain palvelussa ja käyttää protokollia internetiä ei voi käyttää ja se on yksi puute, 
ei voi tehdä kokonaista domain pilveen ja siksi pitää yksi domain, joten kaikkien kaikkien koneen pitää liittyä siihen yhteen domain suhteeseen, sitä tapahtuu työssä

ad domain controlleri virtuaalikoneelle , ad ds authentication voi hyöydnätä virtuaalikone tai jopa mielellään sovelluksessa tai erillinen täys  ad ds pilvelle, ja syitä ei ole miks se on hyvä
azure ad ds microsoft hallinnoima palvelu virtuaalikoneelle authetnitkointai mahdollisuutta, muuten ei saa luottosuhdetta ad protokolla tulee tökkimään

azure ad ds palvelussa 
ad ds on premsisi pilvessä, ja pilvessä oma ad ds palvelu, mielellään on oma premsisi, myös pitää olal nimi palvelut 
myös ad vo iottaa mukaan mobiililaitetta ja pilvi pohjaisia palveluita 

azure ad 

ei ole group policy hallintaa, vaan mdm microsoft intune (kauppa)

ei ole hierkarkioi, vaan yksi ad mollukka, kun ei ole molukka ei luoto suhteita tehtävissä joten eim metsiä


azure ad delekonti,  customer ja pitä huomioida oikeudet ja voi olla hankala kokeilla  mitä pitää tehdä ja mitä oikeuksia puuttuu

administrative units - hallinnan eri tasoja, käyttäjille voi liitää sovelluksia ja käyttöön tietyjä käyttö oikeuksia

delegointi (delegation) malli on eri resursseihin tai voi otta pohjaksi valmiksi rooli ja siitä pohjaisia
liittää tietyihin resurssiin tai pohjaan


azure  multi factor tuo mukaan toisen kerroksen authentication on yleensä  käyttäjä perus tutuuu johonkin kolmeen  (mitä tiedät, mitä omistat ja mitä olet)
